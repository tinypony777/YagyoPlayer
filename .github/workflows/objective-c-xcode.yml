name: Legacy Xcode Analyze (manual only)

on:
  workflow_dispatch:

jobs:
  disabled:
    name: Disabled in favor of Xcode Cloud
    runs-on: ubuntu-latest
    steps:
      - name: Xcode Cloud is the CI source of truth
        run: |
          echo "This legacy workflow is intentionally disabled for push/pull_request."
          echo "Use Xcode Cloud for PR and main build/test validation."

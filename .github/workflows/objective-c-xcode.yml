name: Legacy Xcode Analyze (manual only)

on:
  workflow_dispatch:

jobs:
  disabled:
    name: Disabled (replaced by ios.yml)
    runs-on: macos-15
    steps:
      - run: |
          echo "This workflow is intentionally disabled for push/pull_request."
          echo "Use .github/workflows/ios.yml as the single source of CI truth."

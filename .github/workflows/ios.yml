name: Xcode Cloud Gate

on:
  workflow_dispatch:

jobs:
  xcode-cloud:
    name: Xcode Cloud handles iOS CI
    runs-on: ubuntu-latest
    steps:
      - name: Xcode Cloud is the CI source of truth
        run: |
          echo "Automatic GitHub Actions iOS build/test is intentionally disabled."
          echo "Use Xcode Cloud for PR and main build/test validation."

name: CodeQL (manual only)

on:
  workflow_dispatch:

jobs:
  disabled:
    name: Disabled in favor of Xcode Cloud
    runs-on: ubuntu-latest
    steps:
      - name: Xcode Cloud is the CI source of truth
        run: |
          echo "Automatic GitHub Actions CodeQL analysis is intentionally disabled."
          echo "Use Xcode Cloud for PR and main validation."

# デザインナビ

`docs/PRODUCT_DIRECTION.md`(v3・作者の憲章)に追従するビジュアル・リファレンス。各章の「現在地」を実装コードから起こした画面で裏付け、まだ憲章に書かれていない新規提案を置いておく場所。

**追従対象**: `docs/PRODUCT_DIRECTION.md` v3。ロードマップ・原則・非目標・Decision Rubric などの一次情報はここには複製しない — 常に本体を参照する。憲章が改訂されたら、このファイルの該当節(現在地の画像・翻訳帳プレビュー)を見直す。

**インタラクティブ版**: [Claude Design で見る](https://claude.ai/code/artifact/aa4981af-47ac-4fb9-a764-751f356d6261) — 月タップでの丑三つ時切替に加えて、行列の振付(歩き2フレーム・音の山への跳ね・唐傘と天狗の hit・木魚の squash・狐火の flare・提灯の非対称スムージング)が `YagyoParadeView.swift` の式のまま動く。人魂の浮遊、がしゃどくろの骨鳴らし(パネルタップ)もその場で試せる。Claudeアカウント限定のプライベートリンクなので、他の人に見せたい場合はページ右上の共有メニューから許可が要る。このファイルの画像は、そのアーティファクトから書き出した静止版。

---

## 実装ずみの画面

配色・寸法・ドット絵・レイアウトに加えて、行列の振付(4fpsのフレーム切替・`react = wave² × level` の跳ね・hit/squash/flare・木魚のバチ・強い山の白いひらめき・提灯の非対称スムージング)も `DesignTokens.swift` / `PixelSprites.swift` / `YagyoParadeView.swift` / `ContentView.swift` から直接抽出した、現行実装そのもの(§3・§4.1)。誇張した演出は足していない。

| Day | 丑三つ時(月タップで遷移) |
|---|---|
| ![day](design/screen-day.png) | ![ushimitsu](design/screen-ushimitsu.png) |

## 翻訳帳プレビュー

§4.1 で「将来 `docs/CHOREOGRAPHY.md` として公開・運用される」とされている、音響事象→妖怪の振付の対応表のドラフト。**このファイル自体は `docs/CHOREOGRAPHY.md` ではない** — 正式な発行は Step 4(§5)の release criteria。

| 音響事象 | 妖怪の振付 | 状態 |
|---|---|---|
| 音量の高まり | 行列全体が音量に応じて跳ねる — 固定サイン波 × 音量の疑似ビート | 近似・実装済み |
| 提灯の明滅 | アタックは速く、リリースは遅い非対称スムージングで灯りが揺れる | 実装済み |
| 強打(音量閾値) | 唐傘が開く / 天狗が hit フレームへ切り替わる | 音量閾値の近似 |
| 時刻 2:00 / 月タップ | 丑三つ時へ — 空が紅に染まり、一つ目小僧が最後尾に加わる | 実装済み |
| アタック・無音の検出 | §4.2「一本の耳」の出力で、木魚・天狗・唐傘の反応精度を上げる | 未着手 |
| 聴取史(residency) | 住み着いた妖怪や提灯の色が聴取史を映すようになる | 器のみ・未接続 |
| アートワークの音量 | 人魂がゆらぎ、明滅する(下記・新規提案) | 新規提案 |
| 強いパーカッションの連打 | がしゃどくろの骨が小刻みに震える(下記・新規提案) | 新規提案 |

現在地(正直に): 音響側の語彙はまだ `averagePower` 1語のみ。ラウドネス・無音・オンセットの検出はゼロ — すべて Step 3「一本の耳」を待つ。

## 現在地、早見表

| | |
|---|---|
| スプライト | 行列8体 + 条件出現1体(一つ目小僧) + 未使用1体(ぬりかべ) |
| 歩きフレーム深度 | 2枚(木魚は同一絵の複製で実質1枚)。hitは2体分のみ |
| 音響解析の語彙 | `averagePower` のみ。ラウドネス/無音/オンセットは0 |
| residency の実体 | UUIDハッシュ4行のみ。聴取統計はまだゼロ |
| 狐火の帳(§4.4) | すべて未実装 |

## デザイントークン

`YagyoColor` / `ParadePalette`(DesignTokens.swift)からの抜粋。

![tokens](design/tokens.png)

| 名前 | 用途 | hex |
|---|---|---|
| 墨 sumi | 夜の地 | `#0b0c14` |
| 宵闇 yoiyami | パネル | `#151726` |
| 月白 geppaku | 文字 | `#e9e4d3` |
| 提灯 chochin | アクセント | `#f0a63c` |
| 朱 shu | 通常打 | `#c2402e` |
| 狐火 kitsunebi | ゴースト | `#6fd3e0` |
| 丑三つ時の月 akaMoon | 丑三つ時アクセント | `#d84040` |
| dim | 副次テキスト | `#8a8fa8` |

---

## 新規提案(まだ憲章に無いもの)

どちらも翻訳帳(§4.1)と同じ規律 — 演出のための演出ではなく、必ず音響事象と結びつくこと — で考えた案。

### 人魂 — アートワークの視線

実装済みの円形ウェーブフォームは「音量」を伝えるだけで、民話の語彙ではない。狐火(kitsunebi)と同じ「浮遊する霊」の語彙をアートワークにも持ち込み、提灯と同じ音量明滅の仕組みで動かす案。新規トークンは足さず、既存の狐火/提灯/月白の色を再利用する。

![hitodama](design/hitodama-proposal.png)

### がしゃどくろ — 丑三つ時の奥に、ただ一体

名の由来は骨が鳴る「がしゃがしゃ」という音 — 強いパーカッションの連打に骨が震える、翻訳帳的な必然がある新規キャラクター案。ただし §4.1 は頭数ではなく一体あたりの深度に投資する方針であり、§6 は妖怪の頭数を増やすこと自体を目的にしないと定めている。したがって**行列には加えない**。丑三つ時にだけ、夜空の奥にただ一体 — 月光に沈む頭蓋、眼窩の紅、肋骨の檻、行列の上へ差しかかる指骨 — が気配として佇む。行列の仲間ではなく、丑三つ時という時刻そのものの一部として。

ドット絵は歌川国芳「相馬の古内裏」の骸骨を下敷きに、行列の8体(13〜15ドット)とは別格の大判(約50×56ドット)で起こした2フレーム構成。idle と rattle(下顎・前腕・指骨・肋骨だけを1ドット震わせ、頭蓋は動かさない)を持ち、連打の合図で切り替わる。インタラクティブ版ではパネルに触れると骨が鳴る。

![gashadokuro](design/gashadokuro-proposal.png)

## 狐火の帳(§4.4)コンセプト

現在地はすべて未実装。共存規則(§3)どおり、この画面では夜行絵巻は退き、数値が主役になる。語彙は「補正」ではなく「提案」。

![kitsunebi concept](design/kitsunebi-concept.png)

---

## 更新履歴

- 2026-07-09: がしゃどくろを「骸骨と読める」大判2フレームスプライトに刷新(国芳構図・複数案から審査選定)。インタラクティブ版に行列の振付(2フレーム歩行・hit・squash・flare・バチ・白いひらめき・提灯の非対称スムージング)を実装の式のまま移植し、翻訳帳の各行に妖怪グリフを追加。画像を全て再書き出し。
- 2026-07-08: 初版。PRODUCT_DIRECTION.md v3 をもとに構築したデザインイメージ(Claude Design)から画像を書き出し。人魂・がしゃどくろの新規提案を追加。
